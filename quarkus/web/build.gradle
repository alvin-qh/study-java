plugins {
  id "io.quarkus"
}

dependencies {
  implementation project(":quarkus:util"),
                 "io.quarkus:quarkus-rest:${VER_QUARKUS}",
                 "io.quarkus:quarkus-rest-qute:${VER_QUARKUS}",
                 "io.quarkus:quarkus-rest-jackson:${VER_QUARKUS}",
                 "io.quarkus:quarkus-hibernate-validator:${VER_QUARKUS}"
  testImplementation "io.rest-assured:rest-assured:${VER_REST_ASSURED}"
}
