rootProject.name = "study-java"

include(
  ":se:annotation",
  ":se:binary",
  ":se:collection",
  ":se:concurrent",
  ":se:io",
  ":se:jdbc",
  ":se:mail",
  ":se:process",
  ":se:ratelimit",
  ":se:reflect",
  ":se:runtime",
  ":se:timing",
)
